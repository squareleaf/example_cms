$(".detail-view").html("<%= escape_javascript(render partial: 'edit_contact', locals: { contact: @contact}) %>");

$(".glyphicon-remove").on("click", function(event) {
  event.preventDefault();
  $(".detail-view").toggle();
});

$(".datepicker").datepicker();
